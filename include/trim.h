#include <stdio.h>
#include <string.h>

#ifndef TRIM_H
#define TRIM_H

/// @brief Elimina los espacios al inicio y al final de la cadena s
/// @param s Apuntador a la cadena a aplicar el trim
void trim(char *s);

#endif